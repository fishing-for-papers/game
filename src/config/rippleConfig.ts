export const rippleConfig = {
  // Grid size for clustering nearby points (in pixels)
  clusterGridSize: 50,

  // Cast radius for paper selection (in pixels)
  castRadius: 20,

  // Base radius for ripple circles
  baseRadius: 4,

  // Color and opacity
  color: '#FF6B6B',
  fillOpacity: 0.6,
  strokeWidth: 1,
  strokeOpacity: 0.8,

  // Search parameters
  searchLimit: 100,
  randomSampling: true,
}
